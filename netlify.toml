[build]
  command = "npm run build"
  publish = "dist/client"
  functions = "netlify/functions"

# All routes go to SSR function (static assets are served automatically)
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/ssr"
  status = 200

