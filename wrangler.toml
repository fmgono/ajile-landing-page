name = "ajile-landing-page"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist/client"

# The _worker.js file in dist/client handles SSR
# No need for functions/ directory when using advanced mode
